import CircularProgress from '@mui/material/CircularProgress';

export default function OmProgress() {
    return (
        <div className="om-progress">
            <CircularProgress />
        </div>
    );
}
